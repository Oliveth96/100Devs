#include <stdio.h>
#include <stdlib.h>

int main(int ac, char **av)
{
    int i;
    char *av[];
    av[] = malloc(sizeof(char *) * i);

    for (i = 0; i < av[i] != '\0'; i++)
    {
        printf("%s", av[]);
    }
    return(0);
}